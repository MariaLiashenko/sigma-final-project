import { FC } from "react";
import styles from "./card-banner.module.scss";

const CardBanner: FC = () => (
  <section className={styles.cardBanner}>
    <p>Cart</p>
  </section>
);

export { CardBanner };
